/**
 * @file global.js
 * @description Arquivo responsável por importar e exportar as variáveis globais do projeto.
 */

/**
 * @typedef {import('express').Request} ExpressRequest
 * @typedef {import('express').Response} ExpressResponse
 */

/**
 * @typedef {import('http-status-codes').StatusCodes} StatusCodes
 */

/**
 * @typedef {import('mongoose').Model} Model
 * @typedef {import('mongoose').Document} Document
 * @typedef {import('mongoose').Schema} Schema
 * @typedef {import('mongoose').Types.ObjectId|string} ObjectId
 */
